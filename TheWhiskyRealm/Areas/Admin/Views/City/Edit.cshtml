@model CityFormViewModel

<form asp-area="Admin" asp-controller="City" asp-action="Edit" method="post">
    <div class="form-group">
        <label for="cityName">City Name:</label>
        <input asp-for="Name" class="form-control" style="max-width: 300px;" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label for="zip">ZIP:</label>
        <input asp-for="Zip" class="form-control" style="max-width: 300px;" />
        <span asp-validation-for="Zip" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="CountryId">Country</label>
        <select asp-for="CountryId" asp-items="@(new SelectList(Model.Countries, "Id", "Name"))" class="form-control"></select>
        <span asp-validation-for="CountryId" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-primary">Save Changes</button>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}